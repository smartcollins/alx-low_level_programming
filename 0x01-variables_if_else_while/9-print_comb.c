#include <stdio.h>
/**
 * main - prints numbers separated by ,
 * Return: Always 0
 */
int main(void)
{
	int num;

	for (num = 20; num < 30; num++)
	{
		putchar(num);
		if num != 29
		{
			putchar(',');
			putchar(' ';
					}
					putchar('\n');
					return (0);
					}
