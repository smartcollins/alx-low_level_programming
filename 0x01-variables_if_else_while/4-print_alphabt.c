#include <stdio.h>
/**
 * main - prints alphabet in lower and upper case
 * Return: Always 0
 */
int main(void)
{
	char ch;

	ch = 'a';
	while
		(ch <= 'z') {
			if ((ch != 'q' && ch != 'e') && ch <= 'z')
				putchar(ch);
			c++;
		}
	putchar('\n');
	return (0);
}
