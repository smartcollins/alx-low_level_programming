#include <stdio.h>
/**
 * main - prints alphabet in lower and upper case
 * Return: Always 0
 */
int main(void)
{
	char ch;

	ch = 'a';
	while
		(ch <= 'z'){
			if(( ch != 'q' && c != 'e') && c <= 'z')
				putcahr(c);
			c++;
		}
	putchar('\n');
	return (0);
}
