#include "main.h"
/**
 * _isalpha - checks for lower or upppercase letter
 * @c: walue gotten
 * Return: Always 1 if _isaplha true else 0
*/
int _isalpha(int c)
{
	if ((c >= 'a' || c >= 'A') && (c <= 'z' || c <= 'Z'))
		return (1)
	else
		return (0)
}
